/*
 * Publication configuration. Keep values blank until they are verified.
 * A public form endpoint is not a secret; API keys must never be added here.
 */
window.GrowthSystemsContact = {
  formEndpoint: "",
  contactEmail: "",
  legalBusinessName: "",
};
